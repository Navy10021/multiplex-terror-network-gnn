# Minimal dependency ranges (cross-platform, no CUDA/OS-specific wheels)
numpy>=1.26,<2.1
pandas>=2.2,<2.4
scikit-learn>=1.4,<1.7
matplotlib>=3.8,<3.11
networkx>=3.3,<3.5
pydantic>=1.10,<3

# test runner
pytest>=8,<10

# torch/torch-geometric are intentionally excluded.
# Install them via environment-specific guidance in docs/INSTALL.md.
